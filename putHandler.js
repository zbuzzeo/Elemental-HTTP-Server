'use strict';

const http = require('http');
const fs = require('fs');
const querystring = require('querystring');
const makeTemplate = require('./elementTemplate');

const putHandler = (req, res) => {
  console.log('Switched to putHandler.js');
  
}

module.exports = putHandler;
